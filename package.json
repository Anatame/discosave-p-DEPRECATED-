{
  "name": "extensionparcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist .cache .parcel-cache",
    "build": "parcel build --no-minify src/background.js && parcel build --no-minify src/html/popup.html src/html/signOut.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-plugin-static-files-copy": "^2.6.0"
  },
  "devDependencies": {
    "cssnano": "^5.0.6",
    "postcss": "^8.2.15"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
